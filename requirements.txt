# GIF Converter 執行環境
# 安裝：pip install -r requirements.txt
fastapi==0.140.0
uvicorn==0.51.0
python-multipart==0.0.32
pillow==12.3.0
numpy==2.4.6

# AI 去背（會連帶安裝 onnxruntime / scipy / scikit-image / pymatting）
rembg==2.0.77
onnxruntime==1.28.0

# 打包 exe 時才需要
# pyinstaller==6.22.2
